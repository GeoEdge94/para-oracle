# Foundry compiler output
cache/
out/

# Dependencies (re-installable via `forge install`)
lib/

# Broadcast / deployment logs (keep folder, ignore local runs)
broadcast/*/31337/
broadcast/**/dry-run/

# Docs generated by `forge doc`
docs/

# Env
.env
